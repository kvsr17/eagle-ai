
import Link from 'next/link';

export function Navbar() {
  return (
    <nav className="bg-primary text-primary-foreground shadow-md no-print">
      <div className="container mx-auto px-4 py-3 flex items-center h-12"> {/* Adjusted height to ensure bar is visible */}
        {/* Content removed as per request */}
      </div>
    </nav>
  );
}
